# Real FishKiller Solved Trees

Converted real solver trees live here as plain JSON.

The local server exposes these files through:

```text
/api/solves/real
```

To build the first MVP solve after adding the raw TexasSolver dump:

```powershell
npm run import:k72
```
